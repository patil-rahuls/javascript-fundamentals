## Nullish Coalescing Operator **??**

> It works same as **||** except that it does not consider the following as falsy values:
>
> - **0**
> - **''**
> - **Infinity**
> - **NaN**

Nullish means only **null** and **undefined** will be considered falsy.

> Example 1

```javascript
const usersCount = 0;

const maxUsers = usersCount || 10;

console.log(maxUsers);
// 10

```
_What if we want to consider '0' as a valid value for 'maxUsers' in the example above?_

_Solution - use nullish/coalesce operator._
```javascript
const maxUsersCount = usersCount ?? 10;

console.log(maxUsersCount);
// 0

```

&nbsp;

> Example 2

```javascript
const isDeleted = "";

const result = isDeleted ?? 1;

console.log(result);
// ''

```

&nbsp;

> Example 3: Always use **||** to handle **Infinity** and **NaN**

_Using **??** with **Infinity** and **NaN** is not worth to use, because it will be returning **Infinity** / **NaN**._

```javascript
const minUsers = NaN;
const totalUsers = minUsers ?? 23;
console.log(totalUsers);
// NaN

const users = 1 / 0;
const totalUsrs = users ?? 0;
console.log(totalUsrs);
// Infinity

const minUsrs = minUsers || 23;
console.log(minUsrs);
// 23

```

---
&nbsp;
<!-- PAGINATION_START -->

📁 [1. Basics](../1.%20Basics/)  

◀️ [Logical OR assignment **||=**](06.3-and-or-assignment.md)  

▶️ [Nullish Coalescing Assignment Operator **??=**](06.5-nullish-coalescing-assignment.md)

<!-- PAGINATION_END -->
&nbsp;
