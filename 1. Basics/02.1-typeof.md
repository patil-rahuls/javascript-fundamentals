## **typeof** Operator

```javascript
console.log(typeof x);
// "undefined" (if x isn't defined)

console.log(typeof "arr");
// "string"

console.log(typeof 4);
// "number"

console.log(typeof true);
// "boolean"

// Despite not being numbers, these are considered "number" type.
console.log(typeof NaN);
// "number"
console.log(typeof Infinity);
// "number"

const square = (num) => num * num;
console.log(typeof square);
// "function"

console.log(typeof [2, 4, 6]);
// "object"

console.log(typeof {});
// "object"

```
---
&nbsp;

### How to check if a given object is an Array or an Object?

_Object.prototype.toString.call()_

**Array**

```javascript
const givenVariable = [1, 2, 3];

Object.prototype.toString.call(givenVariable);
// '[object Array]'

```

&nbsp;

**Object**

```javascript
const givenVariable2 = {
  name: "Rahul",
  age: "25",
};

Object.prototype.toString.call(givenVariable2);
// '[object Object]'

```

---

&nbsp;

### Type Validations _(Optional Read)_

> Example : Check if a given string is number

```javascript
function isNumeric(str) {
  if (typeof str != "string") {
    // only process strings!
    return false;
  }

  return 
      !Number.isNaN(str) && 
      !Number.isNaN(Number.parseFloat(str));
}

```

**Number.isNaN** allows whitespaces whereas **Number.parseFloat**/**Number.parseInt** doesn't.

```javascript
Number.isNaN("");       // false
Number.isNaN("   ");    // false

Number.parseInt("");    // NaN
Number.parseInt("   "); // NaN

Number.parseFloat("");  // NaN
Number.parseFloat("  ");// NaN

```

_Note that you can also use these number functions directly, without specifying the **Number** class e.g._

```javascript
return !isNaN(str) && !isNaN(parseFloat(str));

```
_This is because these functions have been around since the earliest versions of JavaScript. In ES6 as a part of modularisation, they were directly copied into the **Number** class._

_By specifying the **Number** class (e.g. **Number.parseFloat**), it explicitly states your intention to work with numbers and follows a better coding standards._

---
&nbsp;
<!-- PAGINATION_START -->

📁 [1. Basics](../1.%20Basics/)

◀️ [Differences - **let**, **const** & **var**](01.5-diff-global-object.md)

▶️ [Type Conversion and Coertion](02.2-type-conversion-&-coercion.md)

<!-- PAGINATION_END -->
&nbsp;
