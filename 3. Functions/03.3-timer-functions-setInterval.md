## Timer Functions - setInterval()

### `setInterval()` calls the callback function for every specified time interval

```javascript
setInterval(() => {
   const now = new Date();
   console.log(now);
}, 1000);
// This will keep printing the date every 1 second to the console.
```

---
